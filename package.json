{
  "name": "tradesafe-app",
  "version": "1.0.0",
  "description": "TradeSafe - Live Trading Signals App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && cd frontend && npm install && npm run build",
    "install-deps": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5",
    "node-cron": "^3.0.2",
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "trading",
    "signals",
    "stocks",
    "real-time",
    "finance",
    "investment"
  ],
  "author": "TradeSafe Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
